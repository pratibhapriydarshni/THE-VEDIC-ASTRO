import {NextResponse} from 'next/server'; import {AccessToken} from 'livekit-server-sdk'; import {authorizeBooking} from '@/lib/consultation-auth';
export async function POST(req:Request){try{const {bookingId}=await req.json(); const a=await authorizeBooking(req,bookingId); const identity=a.user.id; const room=`consultation-${bookingId}`; const token=new AccessToken(process.env.LIVEKIT_API_KEY,process.env.LIVEKIT_API_SECRET,{identity,name:a.user.email||identity,ttl:'1h'}); token.addGrant({room,roomJoin:true,canPublish:true,canSubscribe:true}); return NextResponse.json({token:await token.toJwt(),room,url:process.env.NEXT_PUBLIC_LIVEKIT_URL});}catch(e:any){return NextResponse.json({error:e.message||'token failed'},{status:e.message==='AUTH_REQUIRED'?401:e.message==='FORBIDDEN'?403:400});}}
