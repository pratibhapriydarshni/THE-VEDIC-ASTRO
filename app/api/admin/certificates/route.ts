import {adminDb} from '@/lib/supabase'; import {requireAdmin,apiError} from '@/lib/auth';
export async function GET(req:Request){try{await requireAdmin(req);const {data,error}=await adminDb.from('certificates').select('*').order('created_at',{ascending:false});if(error)throw new Error(error.message);return Response.json({certificates:data||[]})}catch(e){return apiError(e)}}
export async function POST(req:Request){try{const {user}=await requireAdmin(req);const b=await req.json();if(!b.title||!b.storage_path||!b.mime_type)throw new Error('INVALID_CERTIFICATE');const {data,error}=await adminDb.from('certificates').insert({title:b.title,storage_path:b.storage_path,mime_type:b.mime_type,created_by:user.id}).select('*').single();if(error)throw new Error(error.message);return Response.json({certificate:data},{status:201})}catch(e){return apiError(e)}}
export async function DELETE(req:Request){try{await requireAdmin(req);const id=new URL(req.url).searchParams.get('id');if(!id)throw new Error('INVALID_ID');const {error}=await adminDb.from('certificates').delete().eq('id',id);if(error)throw new Error(error.message);return Response.json({ok:true})}catch(e){return apiError(e)}}
